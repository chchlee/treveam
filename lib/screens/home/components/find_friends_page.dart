import 'package:flutter/material.dart';

class FindFreindsPage extends StatelessWidget {
  const FindFreindsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
